# World of DnD Backend

## Database

### Prisma (Supabase)
We're using Prisma as our ORM. The schema is located in `prisma/schema.prisma`. Rename `prisma/schema.example.prisma` to `prisma/schema.prisma` and fill in the database connection string.